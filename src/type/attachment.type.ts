export interface attachment {
  _id: string;
  task: string;
  uploadedBy: string;
  url: string;
  publicId: string;
  filename: string;
  sizeBytes: number;
  mimeType: string;
  createdAt: string;
}
